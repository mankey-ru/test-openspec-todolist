## 1. Setup
- [x] 1.1 Replace Tailwind CDN with Bootstrap 5 CSS CDN in `index.html`
- [x] 1.2 Add `data-bs-theme="dark"` to the `<html>` or `<body>` tag in `index.html`

## 2. Refactoring HTML 
- [x] 2.1 Update static HTML elements in `index.html` to use Bootstrap classes (e.g. `container`, `card`, `text-center`)
- [x] 2.2 Update input and button forms to use Bootstrap `form-control` and `btn btn-primary` classes in `index.html`

## 3. Refactoring JS DOM Generation
- [x] 3.1 Update the element generation in `src/app.js` to output Bootstrap layout classes (like `d-flex`, `justify-content-between`, `align-items-center`)
- [x] 3.2 Update the checkbox and delete button elements to use Bootstrap form and button classes
