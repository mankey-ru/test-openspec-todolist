## 1. Create external stylesheet

- [x] 1.1 Create `src/assets/` directory if it doesn't exist
- [x] 1.2 Create `src/assets/styles.css` with the contents of the `<style>` tag from `index.html`

## 2. Update index.html

- [x] 2.1 Add `<link rel="stylesheet" href="./src/assets/styles.css">` in `<head>` (right after the Bootstrap CDN link)
- [x] 2.2 Remove the `<style>...</style>` block

## 3. Verify

- [x] 3.1 Open `index.html` in browser and confirm visual appearance is identical to before
- [x] 3.2 Verify hover effect on action buttons still works on desktop
